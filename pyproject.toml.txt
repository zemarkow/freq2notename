[build-system]
requires = ["wheel", "setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "freq2notename"
version = "1.0.0"
authors = [
  { name="Zachary E. Markow", email="USE-GITHUB-EMAIL" },
]
description = "Toolkit and graphical interface for converting audio frequencies to musical note names and vice versa"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/FILL-IN-LATER"
Issues = "https://github.com/FILL-IN-LATER/issues"